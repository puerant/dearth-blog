user  nginx;
worker_processes  auto;
error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;

events {
    worker_connections  1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    keepalive_timeout  65;
    client_max_body_size 20M;

    gzip  on;
    gzip_types text/plain text/css application/json application/javascript
               text/xml application/xml text/javascript;

    server {
        listen 80;
        server_name _;

        root /usr/share/nginx/html;
        index index.html;

        # ── API 反向代理 ──────────────────────────────────────────────
        location /api/ {
            proxy_pass         http://backend:8080;
            proxy_http_version 1.1;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
            proxy_read_timeout 60s;
        }

        # ── 上传文件静态访问 ──────────────────────────────────────────
        location /uploads/ {
            alias   /app/uploads/;
            expires 7d;
            add_header Cache-Control "public";
        }

        # ── Admin 管理后台（SPA，base=/admin）────────────────────────
        location ^~ /admin/ {
            root  /usr/share/nginx/html;
            try_files $uri $uri/ /admin/index.html;
        }
        location = /admin {
            return 301 /admin/;
        }

        # ── 文章详情页（SSG 构建时 paths() 只生成 __init__ 占位页）───
        # Nginx 将所有 /articles/* 回退到 __init__ 页，前端读 URL 拿真实 slug
        location /articles/ {
            try_files $uri $uri/ /articles/__init__/index.html;
        }

        # ── Portal 其余页面（SSG + SPA fallback）─────────────────────
        location / {
            try_files $uri $uri/ $uri.html /index.html;
        }
    }

    # ================================================================
    # HTTPS 配置模板（取消注释并填写证书路径后启用）
    # ================================================================
    # server {
    #     listen 443 ssl;
    #     server_name your-domain.com;
    #
    #     ssl_certificate     /etc/nginx/ssl/fullchain.pem;
    #     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    #     ssl_protocols       TLSv1.2 TLSv1.3;
    #     ssl_ciphers         HIGH:!aNULL:!MD5;
    #     ssl_session_cache   shared:SSL:10m;
    #     ssl_session_timeout 10m;
    #
    #     root /usr/share/nginx/html;
    #     index index.html;
    #
    #     location /api/ {
    #         proxy_pass         http://backend:8080;
    #         proxy_http_version 1.1;
    #         proxy_set_header   Host              $host;
    #         proxy_set_header   X-Real-IP         $remote_addr;
    #         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
    #         proxy_set_header   X-Forwarded-Proto $scheme;
    #         proxy_read_timeout 60s;
    #     }
    #
    #     location /uploads/ {
    #         alias   /app/uploads/;
    #         expires 7d;
    #         add_header Cache-Control "public";
    #     }
    #
    #     location ^~ /admin/ {
    #         root  /usr/share/nginx/html;
    #         try_files $uri $uri/ /admin/index.html;
    #     }
    #     location = /admin {
    #         return 301 /admin/;
    #     }
    #
    #     location /articles/ {
    #         try_files $uri $uri/ /articles/__init__/index.html;
    #     }
    #
    #     location / {
    #         try_files $uri $uri/ $uri.html /index.html;
    #     }
    # }
    #
    # # HTTP → HTTPS 重定向
    # server {
    #     listen 80;
    #     server_name your-domain.com;
    #     return 301 https://$host$request_uri;
    # }
}

# dear-blog 项目进度

> 当前阶段：**Phase 6 — 需求迭代**
> 最后更新：2026-03-10

---

## 当前阶段任务

### Phase 6 — 需求迭代

**目标**：基于已上线版本持续迭代需求，按优先级推进功能优化与体验提升。

#### 6.0 字体规范

| # | 任务 | 状态 |
|---|------|------|
| 1 | Portal + Admin 字体规范迭代（本地托管，除 Logo 外统一阿里巴巴普惠体，字重 400/500/700） | 🔄 进行中 |

---

#### 6.1 响应式适配（来源：/adapt 评审，2026-03-10）

| # | 需求描述 | 改动目标 | 优先级 | 状态 |
|---|----------|----------|--------|------|
| R-01 | 移动端汉堡菜单关闭逻辑缺失：点击导航链接后菜单不收起，且汉堡按钮无展开/关闭视觉反馈 | 链接点击后收起菜单；汉堡按钮增加 → × 动画 | P0 | ✅ 已完成 |
| R-02 | 精选文章 featured-grid 在平板尺寸（768px–960px）直接从 3 列跳到 1 列，缺少 2 列过渡断点 | 增加 960px 处 2 列中间断点，640px 以下才降至 1 列，并修正行边框 | P1 | ✅ 已完成 |
| R-03 | 首页各 section 的 `margin-top: 80px` 为内联样式，在移动端偏大；section-hd 的 56px 上内边距在小屏同样冗余 | 内联样式改为 class 管理，移动端 52px / 36px 分级缩减；section-hd 在 768px 以下降为 36px | P1 | ✅ 已完成 |
| R-04 | `.pi-meta` 缺少 `flex-wrap`，项目信息卡在窄屏可能溢出；`.project-info` 移动端顶部边距过大 | pi-meta 加 flex-wrap；project-info 在 768px 以下边距减至 40px | P2 | ✅ 已完成 |

---

#### 6.2 界面设计问题（来源：/critique 评审，2026-03-10）

| # | 需求描述 | 改动目标 | 优先级 | 状态 |
|---|----------|----------|--------|------|
| D-01 | Hero 首屏完全空白：标题与 tagline 代码被注释掉，用户首次进入站点看到的是整屏黑色，无任何文字信息，严重影响第一印象 | 恢复 `hero-display-wrap`，按正确顺序（h1 标题在上，副标题在下）展示站点名称与 tagline | P0 | ✅ 已完成 |
| D-02 | 首页信息架构倒置：项目板块（Projects）位于精选文章和最新文章之前，博客站点应以写作内容为主角，项目为辅 | 调整首页板块顺序：精选文章 → 最新文章 → 项目 | P0 | ✅ 已完成 |
| D-03 | 板块标签语言混用：`// PROJECTS`（英文）与 `// 精选文章`、`// 最新文章`（中文）并列，不一致；附属文案 "View Project →" 亦为英文 | 统一为中文：`// PROJECTS` → `// 项目`，`View Project →` → `查看项目 →` | P1 | ✅ 已完成 |
| D-04 | 板块标签（section-label）对比度过低：使用 `--text-3 (#323130)` 在近黑背景 `#0A0A08` 上，几乎不可辨认 | section-label 颜色改为 `--text-2 (#666260)` 以提升可读性 | P1 | ✅ 已完成 |
| D-05 | 金色强调色（`--accent: #C8A96E`）使用过于泛滥，覆盖导航、徽章、分类标签、标题悬停、项目 ID、进度条等几乎所有元素，失去层级意义 | 收窄强调色：项目编号（pi-id）等纯装饰性文字改为 `--text-3`，仅保留交互态与关键高亮的金色 | P1 | ✅ 已完成 |
| D-06 | 深色模式与 PRD 需求矛盾：PRD 明确要求"极简主义，亮色，无暗色切换"，但当前实现为深色主题 | **决策已定**：同时支持深色与亮色双主题，顶栏增加 Toggle Switch 切换按钮，偏好写入 localStorage 持久化。同步更新 PRD | P0 | ✅ 已完成 |
| D-07 | 页脚品牌名称与导航不一致：Footer 显示 `© 2026 TH`，导航 Logo 为 `DEAR·blog`（DEARth），两处表述不统一 | 在后台站点配置中将 `siteName` 更新为与 Logo 一致的品牌名称 | P2 | ⬜ 待开始 |
| D-08 | 项目状态徽章（ACTIVE / WIP / ARCHIVE）仅通过边框颜色区分，色盲用户无法识别状态差异 | 保留现有文字内容（ACTIVE/WIP/ARCHIVE），在必要时补充视觉辅助（形状/前缀符号）；当前文字本身已具备语义，优先级较低 | P3 | ⬜ 待开始 |
| D-09 | "向下探索" 滚动提示出现在 Hero 底部统计数字之后，用户看到该提示时已处于首屏底部边缘，提示意义减弱 | 评估是否将其前移至统计区域上方，或替换为更具引导性的视觉元素 | P3 | ⬜ 待开始 |

---

#### 6.3 首页 Hero 重设计（来源：设计迭代，2026-03-10）

| # | 需求描述 | 改动目标 | 优先级 | 状态 |
|---|----------|----------|--------|------|
| D-10 | Hero 区域中段（大标题与底部统计之间）完全空白，全屏高度未得到有效利用，视觉重心失衡 | 在 Hero 内嵌入项目索引列表（编号 / 名称 / 年份 / 状态），利用空白区展示个人项目；同时将页面下方的独立项目板块合并入 Hero，减少页面层级 | P1 | ✅ 已完成 |
| D-11 | Hero 大标题动态展示博客站名，与顶栏 Logo 重复；且`--font-display`（普惠体）在超大字号下与 Logo 字体（Syne）脱节，整体字体语言不统一 | Hero 大标题改为静态文字 `DEAR`，字体统一改用 Logo 字体（Syne 800 weight），tagline 副标题从 Hero 移除，简化视觉层次 | P2 | ✅ 已完成 |
| D-12 | Hero 内嵌项目列表字体（普惠体）在小字号下字重过细，辨识度低，与整体 Syne 字体语言不协调 | 项目条目全部改用 `--font-logo`（Syne）：编号/年份 weight 600，名称/状态 weight 700，名称字号从 11px 升至 12px | P2 | ✅ 已完成 |

---

## 已完成

### Phase 5 — 集成联调 & 部署（2026-03-10 完成）

**目标**：前后端完整对接，Docker 一键部署上线。

| # | 任务 | 状态 |
|---|------|------|
| 1 | Admin 与后端接口全量联调 | ✅ 已完成 |
| 2 | Portal 与后端接口全量联调 | ✅ 已完成 |
| 3 | Docker Compose 配置（后端 + MySQL + Redis + Nginx） | ✅ 已完成 |
| 4 | Nginx 配置（反向代理 + 静态文件托管 + HTTPS） | ✅ 已完成 |
| 5 | 生产环境部署验证 | ✅ 已完成 |
| 6 | 功能回归测试 | ✅ 已完成 |

**Admin 联调修复项（2026-03-08）：**
- `PUT /api/admin/article/{id}/status` 端点缺失 → `AdminArticleController` + `ArticleService` 新增
- `VisitTrendVO.date: LocalDate` 序列化为数组 → 添加 `@JsonFormat(pattern = "yyyy-MM-dd")`
- `ProjectSaveRequest.startYear: Integer` → 改为 `String`（对齐实体）
- Admin TS 类型 `ProjectVO.startYear: number` → `string`，`ProjectSaveRequest.startYear?: number` → `string`
- `ProjectView.vue` 提交时做 `String(form.startYear)` 转换，加载时做 `Number(row.startYear)` 转换

**Portal 联调修复项（2026-03-08）：**
- `LocalDateTime publishTime` 全局序列化问题：无 Jackson 配置时序列化为数组 `[2025,3,8,12,0,0]`
- 影响范围：`PortalArticleListVO`、`PortalArchiveVO.ArticleLinkVO`、Admin 所有 VO 的日期字段
- 修复：`application.yml` 添加 `spring.jackson.serialization.write-dates-as-timestamps: false`
- 所有前端 `formatDate(iso)` / `new Date(iso)` 调用均已正确使用 ISO 字符串 ✓
- Portal API 端点与前端调用完全对齐，无其他类型或字段不匹配 ✓

### Phase 4 — Portal 门户（2026-03-08 完成）

**技术栈**：VitePress 1.6.4 + Tailwind CSS 3 + TypeScript + Pinia + Axios + markdown-it + highlight.js

| # | 任务 | 产出 |
|---|------|------|
| 1 | 项目初始化 | `package.json`、`.vitepress/config.ts`、`tailwind.config.js`、`postcss.config.js` |
| 2 | 自定义主题 | `.vitepress/theme/index.ts`、`Layout.vue`（NavBar + Content + Footer） |
| 3 | 设计系统 CSS | `.vitepress/theme/style.css`（完整移植原型暗色编辑风格） |
| 4 | 类型 + API 层 | `src/types/index.ts`、`src/api/portal.ts`（全部 portal 接口） |
| 5 | 状态管理 | `src/stores/site.ts`（站点配置全局状态） |
| 6 | 共享组件 | `NavBar.vue`、`SiteFooter.vue`、`ArticleListItem.vue`、`FeaturedCard.vue`、`ProjectClock.vue`（轨道动画） |
| 7 | 首页 | `pages/HomePage.vue`（Hero + 项目时钟 + 精选 + 最新文章） |
| 8 | 文章详情页 | `pages/ArticleDetailPage.vue`（Markdown/富文本渲染 + TOC + 阅读进度 + 上下篇） |
| 9 | 分类页 | `pages/CategoriesPage.vue`（树形侧边栏 + 文章列表） |
| 10 | 标签页 | `pages/TagsPage.vue`（标签云 + 文章筛选） |
| 11 | 归档页 | `pages/ArchivesPage.vue`（年/月分组时间轴） |
| 12 | 系列页 | `pages/SeriesPage.vue`（展开/折叠系列文章） |
| 13 | 关于页 | `pages/AboutPage.vue`（简介 Markdown 渲染 + 技能标签 + 联系方式） |
| 14 | 路由文件 | `index.md`、`categories.md`、`tags.md`、`archives.md`、`series.md`、`about.md`、`articles/[slug].md` |
| 15 | 构建验证 | `vitepress build` ✓ 2.98s |

### Phase 3 — Admin 管理后台（2026-03-08 完成）

**技术栈**：Vue 3 + Vite 5 + TypeScript + Element Plus 2 + Pinia 2 + Vue Router 4 + Axios + ByteMD + WangEditor 5 + ECharts

| # | 任务 | 产出 |
|---|------|------|
| 1 | 项目初始化 | `package.json`、`vite.config.ts`、`tsconfig.json`、`.env.*`、`index.html` |
| 2 | TS 类型定义 | `src/types/index.ts`（全量接口类型） |
| 3 | API 层 | `src/api/request.ts`（Axios + 401 拦截）、10 个 API 模块 |
| 4 | 状态管理 | `src/stores/auth.ts`（Token + 用户信息） |
| 5 | 路由 | `src/router/index.ts`（路由表 + 登录守卫） |
| 6 | 入口 + 布局 | `main.ts`、`App.vue`、`AdminLayout.vue`、`AuthLayout.vue` |
| 7 | 编辑器组件 | `MarkdownEditor.vue`（ByteMD）、`RichTextEditor.vue`（WangEditor） |
| 8 | 登录页 | `LoginView.vue` |
| 9 | 仪表盘 | `DashboardView.vue`（ECharts 趋势图 + 文章排行） |
| 10 | 文章管理 | `ArticleListView.vue`（筛选分页）、`ArticleEditView.vue`（双模式 + 草稿） |
| 11 | 分类/标签/系列 | `CategoryView.vue`、`TagView.vue`、`SeriesView.vue` |
| 12 | 媒体管理 | `MediaView.vue`（网格/列表 + 上传 + 复制） |
| 13 | 访问统计 | `VisitView.vue`（PV/UV + 趋势图 + 排行） |
| 14 | 项目展示 | `ProjectView.vue` |
| 15 | 个人设置 | `SettingsView.vue`（账号 + 密码 + 站点配置） |

### Phase 2 — 后端接口开发（2026-03-08 完成）

**Request DTOs** (`dto/request/`)

| 文件 | 说明 |
|------|------|
| `LoginRequest.java` | 登录请求 |
| `PasswordUpdateRequest.java` | 修改密码 |
| `UserUpdateRequest.java` | 更新用户信息 |
| `CategorySaveRequest.java` | 分类保存 |
| `TagSaveRequest.java` | 标签保存 |
| `SeriesSaveRequest.java` | 系列保存 |
| `ArticleSaveRequest.java` | 文章保存（含 tagIds） |
| `ProjectSaveRequest.java` | 项目保存（含 techStack/highlights List） |
| `VisitRecordRequest.java` | 访问记录上报 |

**Response VOs** (`dto/response/`)

| 文件 | 说明 |
|------|------|
| `TokenVO.java` | 登录 Token + 过期时长 |
| `UserVO.java` | 用户信息 |
| `CategoryVO.java` | 分类（含 children 递归树） |
| `TagVO.java` | 标签 |
| `SeriesVO.java` | 系列 |
| `ArticleListVO.java` | 文章列表项（不含正文） |
| `ArticleDetailVO.java` | 文章详情（含正文 + tagIds） |
| `MediaVO.java` | 媒体文件 |
| `VisitSummaryVO.java` | PV/UV 摘要 |
| `VisitTrendVO.java` | 单日访问趋势 |
| `ArticleRankVO.java` | 文章浏览量排行 |
| `ProjectVO.java` | 项目展示（含 techStack/highlights List） |
| `portal/PortalTagVO.java` | Portal 标签 |
| `portal/PortalCategoryVO.java` | Portal 分类树 |
| `portal/PortalArticleListVO.java` | Portal 文章列表（含 readingTime） |
| `portal/PortalArticleDetailVO.java` | Portal 文章详情（含 prev/next） |
| `portal/PortalSeriesVO.java` | Portal 系列（列表/详情复用） |
| `portal/PortalArchiveVO.java` | Portal 时间归档（年/月/文章） |
| `portal/PortalHomeVO.java` | Portal 首页聚合数据 |

**Mapper 更新**

| 文件 | 新增方法 |
|------|---------|
| `BlogArticleTagMapper.java` | `deleteByArticleId`、`countPublishedByTagId`、`selectArticleIdsByTagSlug` |
| `BlogVisitLogMapper.java` | `countTotalPv`、`countTotalUv`、`selectTrendSince` |

**Services** (`service/`)

| 文件 | 负责模块 |
|------|---------|
| `AuthService.java` | 登录认证、Token 黑名单 |
| `UserService.java` | 用户信息查询与修改 |
| `SiteConfigService.java` | 站点配置读写 |
| `CategoryService.java` | 分类 CRUD、树形构建、子分类 ID 收集 |
| `TagService.java` | 标签 CRUD |
| `SeriesService.java` | 系列 CRUD |
| `ArticleService.java` | 文章 CRUD、标签关联、计数维护、分页筛选 |
| `MediaService.java` | 文件上传存储、媒体列表与删除 |
| `VisitService.java` | 访问记录、PV/UV 统计、趋势、文章排行 |
| `ProjectService.java` | 项目 CRUD（JSON 字段序列化） |
| `PortalService.java` | Portal 首页、文章列表/详情、分类/标签/系列/归档 |

**Controllers** (`controller/`)

| 文件 | 路由前缀 |
|------|---------|
| `AuthController.java` | `POST /api/auth/login`、`POST /api/auth/logout` |
| `AdminUserController.java` | `GET/PUT /api/admin/user/me`、`PUT /api/admin/user/password` |
| `AdminConfigController.java` | `GET/PUT /api/admin/config` |
| `AdminCategoryController.java` | `GET/POST/PUT/DELETE /api/admin/category` |
| `AdminTagController.java` | `GET/POST/PUT/DELETE /api/admin/tag` |
| `AdminSeriesController.java` | `GET/POST/PUT/DELETE /api/admin/series` |
| `AdminArticleController.java` | `GET/POST/PUT/DELETE /api/admin/article` |
| `AdminMediaController.java` | `POST /api/admin/media/upload`、`GET/DELETE /api/admin/media` |
| `AdminVisitController.java` | `GET /api/admin/visit/summary|trend|article-rank` |
| `AdminProjectController.java` | `GET/POST/PUT/DELETE /api/admin/project` |
| `PortalController.java` | `GET /api/portal/**`、`POST /api/portal/visit` |

---

### Phase 1 — 后端基础框架（2026-03-08 完成）

| # | 任务 | 产出文件 |
|---|------|---------|
| 1 | Maven 多模块项目结构 | `backend/pom.xml`、`blog-common/pom.xml`、`blog-app/pom.xml` |
| 2 | 数据库连接 + Flyway 迁移 | `application.yml`、`db/migration/V1__init.sql` |
| 3 | MyBatis Plus（分页 + 自动填充） | `config/MyBatisPlusConfig.java` |
| 4 | Redis 连接配置 | `config/RedisConfig.java` |
| 5 | JWT + Spring Security 6 | `security/JwtUtil.java`、`JwtAuthFilter.java`、`UserDetailsServiceImpl.java`、`config/SecurityConfig.java` |
| 6 | 统一响应体封装 | `common/result/Result.java`、`ResultCode.java` |
| 7 | 全局异常处理 | `handler/GlobalExceptionHandler.java` |
| 8 | 本地文件存储配置 | `config/WebMvcConfig.java` |
| 9 | SpringDoc OpenAPI 3 | `config/OpenApiConfig.java` |

### Phase 0 — 规划与设计（2026-03-08 完成）

- ✅ PRD 需求文档 → `docs/需求文档/PRD.md`
- ✅ 技术选型文档 → `docs/设计文档/技术选型.md`
- ✅ Portal 原型设计 → `docs/设计文档/原型设计/protal/`
- ✅ 数据库设计 → `docs/设计文档/数据库/init.sql`

---

## 下一阶段预览

### Phase 6 — 需求迭代

Phase 5 已完成，进入持续迭代阶段。核心任务：

- 建立需求池（来源：用户反馈 / 运营诉求 / 技术优化）
- 按双周节奏执行需求评审、开发、测试、发布
- 完善版本变更记录与上线回滚预案

---

## 问题 & 阻塞

> 暂无

---

## 备注

- 状态说明：⬜ 待开始 / 🔄 进行中 / ✅ 已完成 / ❌ 阻塞
- 每完成一个任务，将对应行状态改为 ✅
- 阶段完成后同步更新 `docs/开发文档/里程碑.md` 中对应阶段的状态
